name: camera
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.6
  get: 4.6.6
  http: ^0.13.4 # Để thực hiện các yêu cầu HTTP
  flutter_webview_plugin: ^0.4.0 # Để hiển thị video từ camera Imou
  flutter_vlc_player: ^7.0.0
  # onvif_dart: ^1.1.1
  flutter:
    sdk: flutter
  easy_onvif: ^2.2.0+2
  video_player: ^2.9.1
  network_info_plus: ^3.0.1
  loggy:
  upnp_client: ^0.0.8
  dio: ^5.7.0
  network_discovery: ^1.0.0
  flutter_dotenv: ^5.2.1
  chewie: ^1.8.5
  # permission_handler: ^11.3.1
  # dart_ping: ^9.0.1

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
  - assets/images/
  - assets/.env

